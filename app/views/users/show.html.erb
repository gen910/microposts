<div class="col">
  <aside class="col-md-3">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %><br>
      </h1>
    </section>
    <div>  
        <span><%= link_to "Followings", followings_user_path %></span> 
        <span><%= link_to "Followers", followers_user_path %></span>
    </div>
    <div>  
        <span>Location: <%= @user.location %></span> 
    </div>
  </aside>
   
 　<div class="col-md-8">
    <%= render 'follow_form' if logged_in? %>
    <% if @user.microposts.any? %>
      <h3>Microposts (<%= @user.microposts.count %>)</h3>
      <ol class="microposts">
        <%= render @microposts %>
      </ol>
    <% end %>
  </div>
</div>